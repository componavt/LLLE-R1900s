# Path to source CSV files
CSV_IN_DIR=data/csv_in

# Output directory for cleaned CSV files
CSV_OUT_DIR=data/csv_out

# Fuzzy matching threshold for credit item names (Levenshtein distance)
FUZZY_THRESHOLD=5

# Where to store intermediate parquet file
PARQUET_PATH=data/data.parquet

# Where to store outputs (models, CSVs, etc.)
OUTPUTS_DIR=outputs

